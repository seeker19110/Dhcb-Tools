bin/
obj/
.vs/
*.user
*.suo
__pycache__/

# Rác do build target của package AutoCAD.NET sinh ra ở gốc repo.
build_temp/
build_v2/

# Gói phát hành dựng tại chỗ: zip/exe của release.yml và .mcpb của scripts/pack-mcpb.ps1.
dist/

# Worktree tạm của agent (Claude Code) — không phải mã nguồn
.claude/worktrees/

# Dữ liệu đo phủ (coverage.py và coverlet) — sinh ra mỗi lần chạy test, không đưa vào repo.
.coverage
coverage/
TestResults/
