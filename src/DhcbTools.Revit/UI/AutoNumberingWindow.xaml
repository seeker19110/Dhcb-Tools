<Window x:Class="DhcbTools.Revit.UI.AutoNumberingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DHCB Tools - Đánh số tự động"
        Width="380" SizeToContent="Height" WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <StackPanel Margin="16">
        <TextBlock Text="Category (ví dụ: Doors, Rooms)" Margin="0,0,0,4"/>
        <TextBox x:Name="CategoryBox" Margin="0,0,0,12" Text="Doors"/>

        <TextBlock Text="Tham số ghi số (mặc định: Mark)" Margin="0,0,0,4"/>
        <TextBox x:Name="ParameterBox" Margin="0,0,0,12" Text="Mark"/>

        <TextBlock Text="Tiền tố (ví dụ: D-)" Margin="0,0,0,4"/>
        <TextBox x:Name="PrefixBox" Margin="0,0,0,12" Text=""/>

        <Grid Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="Số bắt đầu"/>
                <TextBox x:Name="StartNumberBox" Text="1"/>
            </StackPanel>
            <StackPanel Grid.Column="2">
                <TextBlock Text="Số chữ số (0 = không đệm)"/>
                <TextBox x:Name="PadWidthBox" Text="0"/>
            </StackPanel>
        </Grid>

        <CheckBox x:Name="DryRunCheck" Content="Chỉ xem trước (không ghi vào mô hình)" IsChecked="True" Margin="0,0,0,16"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CancelButton" Content="Huỷ" Width="80" Margin="0,0,8,0" Click="OnCancel"/>
            <Button x:Name="RunButton" Content="Chạy" Width="80" IsDefault="True" Click="OnRun"/>
        </StackPanel>
    </StackPanel>
</Window>
