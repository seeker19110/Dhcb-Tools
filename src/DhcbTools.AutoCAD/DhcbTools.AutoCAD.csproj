<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Library</OutputType>
    <RootNamespace>DhcbTools.AutoCAD</RootNamespace>
    <AssemblyName>DhcbTools.AutoCAD</AssemblyName>
    <UseWPF>true</UseWPF>
    <CopyLocalLockFileAssemblies>true</CopyLocalLockFileAssemblies>
    <DefineConstants Condition="'$(DhcbSkipWpf)'=='true'">$(DefineConstants);DHCB_NO_WPF</DefineConstants>
  </PropertyGroup>

  <ItemGroup>
    <!-- Build kiểm tra không WPF (Linux/CI): Ribbon dựng bằng WPF (System.Windows.Media) nên bỏ ra,
         mọi thứ còn lại vẫn phải biên dịch. Chỗ gọi trong App.cs guard bằng DHCB_NO_WPF. -->
    <Compile Remove="Ribbon\**" Condition="'$(DhcbSkipWpf)'=='true'" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\DhcbTools.Core.AutoCAD\DhcbTools.Core.AutoCAD.csproj" />
    <!-- Cho phép vỏ AutoCAD dùng HttpBridgeServer chung (token, khoá dò token, /tools, /chat). -->
    <ProjectReference Include="..\DhcbTools.Shared.Hosting\DhcbTools.Shared.Hosting.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="AutoCAD.NET" Version="$(AcadNetVersion)" />
    <PackageReference Include="Newtonsoft.Json" Version="13.0.3" />
  </ItemGroup>
  <ItemGroup>
    <!--
      snapshot (10.1) trả System.Drawing.Bitmap: net48 có System.Drawing trong hộp, còn net8/net10 phải
      kéo System.Drawing.Common — AutoCAD.NET không khai dependency này dù AcDbMgd trả Bitmap.
      Không dùng "Reference" cho net8+ (không có assembly đó trong runtime pack) và không dùng package
      cho net48 (thừa, và kéo cả bộ phụ thuộc .NET Core vào add-in .NET Framework).
    -->
    <Reference Include="System.Drawing" Condition="'$(TargetFramework)'=='net48'" />
    <PackageReference Include="System.Drawing.Common" Version="8.0.*" Condition="'$(TargetFramework)'=='net8.0-windows'" />
    <!--
      AcDbMgd 25.1 (AutoCAD 2026, net10) tham chiếu System.Drawing.Common 10.0.0.0 — bản 8.0 gây CS1705.
      NoWarn NU1510: SDK 10 báo "package tự động có sẵn, bỏ PackageReference đi", nhưng bỏ thật thì được
      CS1069 ("type Bitmap đã forward sang System.Drawing.Common, thêm reference đi") — đã thử. Gói này chỉ
      tự có khi project dùng WindowsDesktop.App (WinForms/WPF), mà vỏ core-only thì không.
    -->
    <PackageReference Include="System.Drawing.Common" Version="10.0.*" Condition="'$(TargetFramework)'=='net10.0-windows'" />
  </ItemGroup>

  <!-- NU1510 là cảnh báo lúc restore nên phải tắt ở PropertyGroup, không tắt được trên PackageReference. -->
  <PropertyGroup Condition="'$(TargetFramework)'=='net10.0-windows'">
    <NoWarn>$(NoWarn);NU1510</NoWarn>
  </PropertyGroup>

</Project>
