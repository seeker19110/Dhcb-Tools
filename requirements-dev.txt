# Công cụ kiểm tra phần Python (scripts/, tools/autocad-mcp-server/). Không cần để CHẠY script.
#   pip install -r requirements-dev.txt
# Kéo theo fastmcp: test_panel_api.py nhập server.py (kiểm cổng xác nhận của MCP), thiếu nó là
# cả bộ test không chạy được ca nào chứ không phải bỏ qua vài ca.
-r tools/autocad-mcp-server/requirements.txt
pytest>=8,<10
pyflakes>=3,<4
# Cổng phủ 100% câu lệnh (ngưỡng ở [tool.coverage.report] trong pyproject.toml).
coverage>=7,<8
